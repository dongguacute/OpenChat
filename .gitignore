node_modules
.turbo
dist
dist-ssr
.vercel
.env
.env*.local
*.log
.DS_Store
.pnpm-store

# Supabase CLI — local branching state & temp (still commit migrations, config.toml, functions)
**/supabase/.branches
**/supabase/.temp
